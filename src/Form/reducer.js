/* eslint-disable require-jsdoc */
import {
  async,
} from './actions';

const {
} = async;

const initialState = {
};

export default function Reducer(state=initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
